package com.example.quizztezz.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.quizztezz.R

object OnestFont {
    val OnestBoldFamily = FontFamily(Font(R.font.onestbold))
    val OnestBlackFamily = FontFamily(Font(R.font.onestblack))
    val OnestExtraBoldFamily = FontFamily(Font(R.font.onestextrabold))
    val OnestLightFamily = FontFamily(Font(R.font.onestlight))
    val OnestMediumFamily = FontFamily(Font(R.font.onestmedium))
    val OnestRegularFamily = FontFamily(Font(R.font.onestregular))
    val OnestThinFamily = FontFamily(Font(R.font.onestthin))

}